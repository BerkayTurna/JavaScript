const Rectangle = ({ color }) => {
  return (
    <div
      style={{
        height: "50px",
        width: "100px",
        backgroundColor: color,
        margin: "10px",
      }}
    ></div>
  );
};

export default Rectangle;
