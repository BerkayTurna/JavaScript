const Square = ({ color }) => {
  return (
    <div
      style={{
        height: "50px",
        width: "50px",
        backgroundColor: color,
        margin: "10px",
      }}
    ></div>
  );
};

export default Square;
